const connect = require ('../database/conection');
const crypto = require ('crypto');
const data = new Date();


module.exports = {
    async create (req, res){
       
        const CreatedAt = DataAt.default.dd   
            return res.json(CreatedAt)
    }
}